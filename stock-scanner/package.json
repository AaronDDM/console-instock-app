{
  "name": "stock-scanner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cp package.json ./.build/package.json && esbuild lambda/index.ts --bundle --platform=node --external:aws-sdk --outfile=./.build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.0",
    "aws-sdk": "^2.984.0",
    "esbuild": "^0.13.7",
    "typescript": "^4.4.4"
  }
}
